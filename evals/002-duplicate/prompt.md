We have a ledger (left.csv) and bank statement (right.csv). The ledger sometimes records the same payment twice by accident (duplicates). Please create a `reconify.yaml` configuration to reconcile these files.

The columns are: date, amount, currency, reference, description. Date layout is '2006-01-02'.